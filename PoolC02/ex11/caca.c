void to_base_alt(char *str, int n, int s, char *base)
{
	const int	b = ft_strlen(base);
	int			len;
	int			tmp;
	int			isneg;

	len = 0;
	tmp = n;
	isneg = 0;
	if (n == 0)
		str[len++] = '0';
	else
	{
		while(tmp != 0)
		{
			
		}
	}
}
